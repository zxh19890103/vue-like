/**
 * State always be held by component instance.
 * State can be bound to template
 * 
 * Every component has a render method whilch returns the dom descriptor.
 * 
 * While rendering, we use the states and props (state in the parent component or state.) to generate a DOM.
 * While state or props updating, we use RXJS to notify the DOM to update. 
 */

 